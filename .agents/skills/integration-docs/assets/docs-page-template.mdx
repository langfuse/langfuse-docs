---
title: "Trace <Host> with Langfuse"
sidebarTitle: <Host>
logo: /images/integrations/<host>_icon.svg
logoAppearance: dark        # omit unless the logo needs it
description: "Trace <Host> coding sessions — <the signals> — with Langfuse for comprehensive observability."
category: Integrations
---

# <Host> tracing with Langfuse

<!-- Sentence case in every heading. Evidence: claude-code.mdx and kiro.mdx use
     Title Case; every other sibling page uses sentence case. -->

## What can this integration trace?

<!-- Same list as the repo README, same order. -->

## How it works

## Quick start

### Set up Langfuse

### Install the plugin

<!-- Add the [#enable-the-plugin] anchor where a sibling page links to it. -->

### Add your Langfuse credentials [#set-credentials]

<!-- Keep the [#set-credentials] anchor: sibling pages deep-link to it. -->

### Verify tracing

<!-- Rename after what the reader actually does — `Restart Codex and verify
     tracing`, `Use OpenCode`, `Run your first trace`. Close with the exact host
     output line that means the integration ran, plus one bounded, copy-pasteable
     read command that proves traces landed. -->

### View traces in Langfuse

## Environment variables

## Enable and disable tracing

<!-- Where the integration has a switch. Evidence: users asked for an off
     switch. Drop this section only when there is nothing to switch. -->

## Troubleshooting

### No traces appearing in Langfuse

### Authentication errors

### Data privacy

<!-- A Troubleshooting subsection, not a top-level section — codex.mdx and
     opencode.mdx place it here. State which data classes are sent, which flags
     control each, the defaults, what is redacted, and where it goes. -->

## Resources

```mermaid
flowchart TB
    User["UI, API, SDKs"]
    subgraph vpc["VPC"]
        Web["Web Server (langfuse/langfuse)"]
        Postgres@{ img: "https://upload.wikimedia.org/wikipedia/commons/2/29/Postgresql_elephant.svg", label: "Postgres Database", pos: "b", w: 60, h: 60, constraint: "on" }
    end
    LLM["LLM API/Gateway<br/>(optional; BYO; can be same VPC or VPC-peered)"]

    User --> Web
    Web --> Postgres
    Web -.->|"optional for playground"| LLM
```

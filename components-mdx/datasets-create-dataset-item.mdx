<Tabs items={["Python SDK", "JS/TS SDK", "Langfuse UI"]}>
<Tab>

```python
langfuse.create_dataset_item(
    dataset_name="<dataset_name>",
    # any python object or value, optional
    input={
        "text": "hello world"
    },
    # any python object or value, optional
    expected_output={
        "text": "hello world"
    },
    # metadata, optional
    metadata={
        "model": "llama3",
    }
)
```

_See [Python SDK v3](/docs/sdk/python/sdk-v3) docs for details on how to initialize the Python client._

</Tab>
<Tab>

```ts
import { LangfuseClient } from "@langfuse/client"

const langfuse = new LangfuseClient()

await langfuse.api.datasetItems.create({
  datasetName: "<dataset_name>",
  // any JS object or value
  input: {
    text: "hello world",
  },
  // any JS object or value, optional
  expectedOutput: {
    text: "hello world",
  },
  // metadata, optional
  metadata: {
    model: "llama3",
  },
});
```

_See [JS/TS SDK](/docs/sdk/typescript/guide) docs for details on how to initialize the JS/TS client._

</Tab>

<Tab>
<Tabs items={["Add item", "Import CSV", "Add from trace"]}>

<Tab>
<CloudflareVideo
  videoId="f185bc7dff9b886d0c3468ab0852cbb5"
  aspectRatio={16 / 9}
  gifStyle
/>
</Tab>

<Tab>
<CloudflareVideo
  videoId="356c0afb92cbb5715c0eccc5fa30ae16"
  aspectRatio={16 / 9}
  gifStyle
/>
</Tab>

<Tab>
<CloudflareVideo
  videoId="51ba46cebd0431f9e5483082a8470206"
  aspectRatio={16 / 9}
  gifStyle
/>
</Tab>

</Tabs>

</Tab>

</Tabs>

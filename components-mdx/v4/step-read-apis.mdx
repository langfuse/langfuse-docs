**Read APIs:** move reads from the v1 endpoints (`api.legacy.*` or direct REST calls) to the [Observations API v2](/docs/api-and-data-platform/features/observations-api) and [Metrics API v2](/docs/metrics/features/metrics-api). The [full endpoint list with replacements](/docs/compatibility#sdk-server) is in the compatibility matrix.

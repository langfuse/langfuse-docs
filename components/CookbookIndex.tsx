import { Callout } from "nextra/components";

export const CookbookIndex = () => (
  <Callout type="warning">
    Cookbook index temporarily disabled during Nextra v4 migration. See Backlog.md.
  </Callout>
);

import { Callout } from "nextra/components";

// TODO(nextra v4): Re-implement using nextra/page-map
export const VideoIndex = () => (
  <Callout type="warning">
    Video index temporarily disabled during Nextra v4 migration. See Backlog.md.
  </Callout>
);

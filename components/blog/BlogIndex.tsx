import { Callout } from "nextra/components";

export const BlogIndex = () => (
  <Callout type="warning">
    Blog index temporarily disabled during Nextra v4 migration. See Backlog.md.
  </Callout>
);

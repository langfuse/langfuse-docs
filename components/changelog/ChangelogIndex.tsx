import { Callout } from "nextra/components";

export const ChangelogIndex = () => (
  <Callout type="warning">
    Changelog index temporarily disabled during Nextra v4 migration. See Backlog.md.
  </Callout>
);

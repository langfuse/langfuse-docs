import { HomeSection } from "./components/HomeSection";
import { Header } from "../Header";

export default function ContinuousEvaluationLoop() {
  return (
    <HomeSection className="flex flex-col items-center">
      <Header
        title="Continuous Evaluation Loop"
        description={<span>// TODO: Add description</span>}
        className="mb-0"
      />
      {/* TODO: Add content */}
    </HomeSection>
  );
}

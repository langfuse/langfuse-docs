### 1. Tracing

<span>API & SDKs for Python & Typescript</span>

```python filename="server.py"
# LLM-based server route
trace = langfuseClient.trace.create( request = {
    "name": "chat-completion",
    "attributes": {
        "prompt": "Hi there!",
        "completion": "Hi 👋",
        "model": ...
    },
    ...
}))
```

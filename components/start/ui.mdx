import { Tab, Tabs } from "nextra-theme-docs";
import { Screenshot } from "@/components/screenshot";
import screenDashboard from "@/src/screen_dashboard.png";
import screenSetup from "@/src/screen_setup.png";

### 3. UI

<Tabs items={["Dashboard", "Setup", "API"]}>
  <Tab>
    <Screenshot src={screenDashboard} />
  </Tab>
  <Tab>
    <Screenshot src={screenSetup} />
  </Tab>
  <Tab>
    <Screenshot src={screenDashboard} />
  </Tab>
</Tabs>

---
title: "Upgrade to Langfuse v4"
description: Langfuse v4 introduces the observations-first data model. Find your situation, see the exact steps and deadlines, and upgrade.
---

import { Book, Github, Table, GitCompare } from "lucide-react";
import { Details, Summary } from "@/components/Details";
import { VersionTimeline } from "@/components/VersionTimeline";
import { Steps, Step } from "@/components/docs";
import { ObservationsTableDiagram } from "@/components/ObservationsTableDiagram";
import V4StepReadApis from "@/components-mdx/v4/step-read-apis.mdx";
import V4StepEvaluators from "@/components-mdx/v4/step-evaluators.mdx";
import V4StepExports from "@/components-mdx/v4/step-exports.mdx";
import V4StepUiToggle from "@/components-mdx/v4/step-ui-toggle.mdx";
import V4StepVerify from "@/components-mdx/v4/step-verify.mdx";

# Langfuse v4

Langfuse v4 rebuilds the data model around observations: trace-level attributes live on every observation, and records are immutable (written once, never updated). This removes expensive joins and the deduplication of updated records at read time, making queries an order of magnitude faster at scale. This page is the starting point for upgrading. Find [your situation](#what-you-need-to-do) below, follow the steps, and check the [Versions & Compatibility](/docs/compatibility) page for the full matrix.

<Callout type="info">
New to Langfuse? There is nothing to migrate. Start with the [get started guide](/docs/observability/get-started); it already uses v4.
</Callout>

## Timeline [#timeline]

<VersionTimeline />

| Date                    | What happens                                                                                               | Who is affected                                                           |
| ----------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **August 1, 2026**      | v4 ships for self-hosted deployments                                                                       | Self-hosters                                                              |
| **End of October 2026** | Langfuse Cloud switches to v4 as the only experience; the "Preview" toggle is removed                      | All Langfuse Cloud users                                                  |
| **November 30, 2026**   | Legacy surfaces are removed on Langfuse Cloud: v1 read APIs, legacy trace ingestion, legacy export sources | Cloud users on Python SDK v2 / JS SDK v3, v1 read APIs, or legacy exports |

On Langfuse Cloud, review your project's remaining migration actions directly [in the app](https://cloud.langfuse.com/project/~/v4).

## What you need to do [#what-you-need-to-do]

**Upgrading your SDKs is the most important step**; everything else builds on it. Not sure which combinations are supported? The [compatibility matrix](/docs/compatibility#sdk-server) shows every SDK, API, and export against each server version.

### Langfuse Cloud with older SDKs (Python v2 / JS v3 or older) [#cloud-older-sdks]

**Deadline: November 30, 2026.** These SDKs send traces via the legacy ingestion API, which is removed on that date; trace ingestion stops working. Score ingestion is unaffected.

<Steps>

<Step>

**Ingestion**

Upgrade to [Python SDK v4](/docs/observability/sdk/upgrade-path/python-v3-to-v4) or [JS/TS SDK v5](/docs/observability/sdk/upgrade-path/js-v3-to-v4) (the guides cover each major-version hop), and replace `update_current_trace()` / `updateActiveTrace()` with `propagate_attributes()`.

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

<Step>

<V4StepUiToggle />

</Step>

<Step>

<V4StepVerify />

</Step>

</Steps>

### Langfuse Cloud with current SDKs (Python v3+ / JS v4+) [#cloud-current-sdks]

**Deadline: November 30, 2026** for the v1 read APIs, trace-level evaluators, and legacy exports; the SDK upgrade itself is recommended before the end of October 2026 so your data stays real time.

<Steps>

<Step>

**Ingestion**

Upgrade to Python SDK ≥ 4.7.0 / JS SDK ≥ 5.4.0 (below these versions, data appears with up to a 10-minute delay on the v4 data model) and replace `update_current_trace()` / `updateActiveTrace()` with `propagate_attributes()` ([Python guide](/docs/observability/sdk/upgrade-path/python-v3-to-v4), [JS guide](/docs/observability/sdk/upgrade-path/js-v4-to-v5)).

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

<Step>

<V4StepUiToggle />

</Step>

<Step>

<V4StepVerify />

</Step>

</Steps>

### Self-hosted deployments [#self-hosted]

**No forced deadline.** v4 ships by August 1, 2026; v3 continues to receive security patches. Upgrade at your own pace, in this order:

<Steps>

<Step>

**Ingestion**

Upgrade SDKs first: [Python SDK v4](/docs/observability/sdk/upgrade-path/python-v3-to-v4) / [JS/TS SDK v5](/docs/observability/sdk/upgrade-path/js-v4-to-v5). Python SDK v2 and JS SDK v3 cannot send traces to a v4 server.

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

<Step>

**Server**

Upgrade from v3 to v4 following the [upgrade documentation](/self-hosting/upgrade). The v4 UI ships with the server; no toggle is needed.

</Step>

<Step>

<V4StepReadApis />

</Step>

</Steps>

### Direct API and OpenTelemetry users [#api-otel]

**Deadline: November 30, 2026** for legacy trace ingestion and the v1 read APIs on Langfuse Cloud.

<Steps>

<Step>

**Ingestion**

Everyone still sending trace, span, or generation events to the legacy `POST /api/public/ingestion` API must move to the [OpenTelemetry endpoint](/integrations/native/opentelemetry) `POST /api/public/otel/v1/traces`. Set the `x-langfuse-ingestion-version: 4` header on your span exporter for real-time data, and propagate trace attributes (`user_id`, `session_id`, ...) to all spans.

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

</Steps>

## What changed [#what-changed]

The new mental model: Langfuse stores one immutable observations table. A trace is simply all rows that share a `trace_id`, and trace-level attributes are copied onto every row:

<ObservationsTableDiagram />

| Before (v3)                                                          | After (v4)                                                                                                                                        |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Separate, mutable traces and observations tables                     | Single immutable observations table; a trace is the set of observations sharing a `trace_id`                                                      |
| Trace attributes stored on the trace, joined at query time           | Trace attributes live on every observation, propagated by the SDKs                                                                                |
| `update_current_trace()` / `updateActiveTrace()`                     | `propagate_attributes()`                                                                                                                          |
| Trace input/output                                                   | Observation input/output (`set_trace_io` is deprecated)                                                                                           |
| v1 read APIs (observations, metrics, traces, sessions, scores v1/v2) | [Observations API v2](/docs/api-and-data-platform/features/observations-api), [Metrics API v2](/docs/metrics/features/metrics-api), Scores API v3 |
| Legacy batch ingestion for trace, span, and generation events        | OTLP endpoint `POST /api/public/otel/v1/traces`                                                                                                   |
| Trace-level LLM-as-a-judge evaluators                                | [Observation-level evaluators](/faq/all/llm-as-a-judge-migration)                                                                                 |
| Export source "traces and observations"                              | Export source "enriched observations"                                                                                                             |

Why observations-first: agentic applications produce traces with hundreds of operations, and the operations engineers care about were hidden inside one trace row. With observations as the primary entity you query the operations directly ("which LLM calls are slow?"), charts load faster over large time ranges, and observation-level evaluations run in seconds. The [data model docs](/docs/observability/data-model) explain the model; the [observations guide](/faq/all/explore-observations-in-v4) covers day-to-day workflows in the UI.

<Details>
<Summary>Cloud rollout details (until the end of October 2026)</Summary>

- Organizations created on or after April 14, 2026, 14:00 UTC run v4 by default and do not see a toggle.
- Organizations created before that date can switch the **Preview** toggle (bottom-left in the UI) on and off until Langfuse Cloud becomes v4-only at the end of October 2026.
- Cloud projects created on or after May 20, 2026 are locked to the "enriched observations" export source; new legacy export integrations cannot be created since June 22, 2026.

</Details>

## Questions [#questions]

Ask rollout and migration questions in the dedicated [GitHub Discussion](https://github.com/orgs/langfuse/discussions/12518); we use that thread for updates and answers.

<Cards num={2}>
  <Card
    title="SDK migration guides"
    href="/docs/observability/sdk/upgrade-path"
    icon={<Book />}
  />
  <Card
    title="Versions & Compatibility"
    href="/docs/compatibility"
    icon={<GitCompare />}
  />
  <Card
    title="Working with observations"
    href="/faq/all/explore-observations-in-v4"
    icon={<Table />}
  />
  <Card
    title="Ask questions on Langfuse v4"
    href="https://github.com/orgs/langfuse/discussions/12518"
    icon={<Github />}
  />
</Cards>

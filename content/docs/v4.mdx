---
title: "Upgrade to Langfuse v4"
description: Langfuse v4 introduces the observations-first data model. Find your situation, see the exact steps and deadlines, and upgrade.
---

import { Book, Github, Table, GitCompare } from "lucide-react";
import { Details, Summary } from "@/components/Details";
import { VersionTimeline } from "@/components/VersionTimeline";
import { Steps, Step } from "@/components/docs";
import { ObservationsTableDiagram } from "@/components/ObservationsTableDiagram";
import V4StepReadApis from "@/components-mdx/v4/step-read-apis.mdx";
import V4StepEvaluators from "@/components-mdx/v4/step-evaluators.mdx";
import V4StepExports from "@/components-mdx/v4/step-exports.mdx";
import V4StepUiToggle from "@/components-mdx/v4/step-ui-toggle.mdx";
import V4StepVerify from "@/components-mdx/v4/step-verify.mdx";

# Langfuse v4

Langfuse v4 rebuilds the data model around observations: trace-level attributes live on every observation, and records are immutable (written once, never updated). Reads no longer need joins or deduplication, so queries run an order of magnitude faster at scale. This page is the starting point for upgrading. Find [your situation](#what-you-need-to-do) below, follow the steps, and check the [Versions & Compatibility matrix](/docs/compatibility#sdk-server) for what works with what.

<Callout type="info">
New to Langfuse? There is nothing to migrate. Start with the [get started guide](/docs/observability/get-started); it already uses v4.
</Callout>

## Timeline [#timeline]

<VersionTimeline />

On Langfuse Cloud, review your project's remaining [migration actions in the app](https://cloud.langfuse.com/project/~/v4).

## What you need to do [#what-you-need-to-do]

**Upgrading your SDKs is the most important step**; everything else builds on it. Not sure which combinations are supported? The [compatibility matrix](/docs/compatibility#sdk-server) shows every SDK, API, and export on Langfuse Cloud; self-hosters use the [self-hosted matrix](/self-hosting/upgrade/versioning#sdk-server).

### Langfuse Cloud with older SDKs (Python v2 / JS v3 or older) [#cloud-older-sdks]

**Deadline: will be announced.** These SDKs send traces via the legacy ingestion API, which is removed when Langfuse Cloud switches to v4; trace ingestion then stops working. Score ingestion is unaffected.

<Steps>

<Step>

**Ingestion**

Upgrade to the latest SDK majors, Python v4 and JS/TS v5; the [SDK upgrade paths](/docs/observability/sdk/upgrade-path) cover each major-version hop. Then replace `update_current_trace()` / `updateActiveTrace()` with `propagate_attributes()`.

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

<Step>

<V4StepUiToggle />

</Step>

<Step>

<V4StepVerify />

</Step>

</Steps>

### Langfuse Cloud with current SDKs (Python v3+ / JS v4+) [#cloud-current-sdks]

**Deadline: will be announced** for the v1 read APIs, trace-level evaluators, and legacy exports; upgrade your SDKs now so your data stays real time.

<Steps>

<Step>

**Ingestion**

Upgrade to Python SDK ≥ 4.7.0 / JS SDK ≥ 5.4.0 (below these versions, data appears with up to a 10-minute delay on the v4 data model) and replace `update_current_trace()` / `updateActiveTrace()` with `propagate_attributes()` ([Python guide](/docs/observability/sdk/upgrade-path/python-v3-to-v4), [JS guide](/docs/observability/sdk/upgrade-path/js-v4-to-v5)).

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

<Step>

<V4StepUiToggle />

</Step>

<Step>

<V4StepVerify />

</Step>

</Steps>

### Direct API and OpenTelemetry users [#api-otel]

**Deadline: will be announced** for legacy trace ingestion and the v1 read APIs on Langfuse Cloud.

<Steps>

<Step>

**Ingestion**

Everyone still sending trace, span, or generation events to the legacy `POST /api/public/ingestion` API must move to the [OpenTelemetry endpoint](/integrations/native/opentelemetry) `POST /api/public/otel/v1/traces`. Set the [`x-langfuse-ingestion-version: 4` header](/integrations/native/opentelemetry#real-time-ingestion) on your span exporter for real-time data, and [propagate trace attributes](/integrations/native/opentelemetry#propagating-attributes) (`user_id`, `session_id`, ...) to all spans.

</Step>

<Step>

<V4StepReadApis />

</Step>

<Step>

<V4StepEvaluators />

</Step>

<Step>

<V4StepExports />

</Step>

</Steps>

### Self-hosted deployments [#self-hosted]

**No forced deadline.** v3 continues to receive security patches, and breaking removals only happen in major server releases. Follow the [GitHub discussion](https://github.com/orgs/langfuse/discussions/14157) for the self-hosted v4 go-live date; the migration guide for self-hosted deployments ships with the release. Until then, the [self-hosted compatibility matrix](/self-hosting/upgrade/versioning#sdk-server) shows what each server version supports.

## What changed [#what-changed]

The new mental model: Langfuse stores one immutable observations table. A trace is simply all rows that share a `trace_id`, and trace-level attributes are copied onto every row:

<ObservationsTableDiagram />

**Trace-level input/output is deprecated across the product.** Every feature that previously read a trace's input and output (tables in the UI, LLM-as-a-judge evaluators, exports) now reads the input and output of the right observation, typically the root span or the LLM call that holds the relevant data. Selecting one observation with the right data instead of assembling it across a whole trace is a big part of the faster loading times.

Why observations-first: two problems drove this change. First, agentic traces got big, and a single trace input/output is no longer what you are interested in: you care about a specific sub-agent, a specific tool call, or a specific LLM call inside the run. With observations as the primary entity you query those directly ("which LLM calls are slow?"), and observation-level evaluations run in seconds. Second, scalability: applications continuously updated the trace output as the agent progressed, and every update created another record version that Langfuse had to deduplicate at read time, degrading performance across the product. Immutable observations remove that cost. The [data model docs](/docs/observability/data-model#observations-and-traces) explain the model; the [observations guide](/faq/all/explore-observations-in-v4) covers day-to-day workflows in the UI.

<Details>
<Summary>Cloud rollout details (until the v4 cutover)</Summary>

- Organizations created on or after April 14, 2026, 14:00 UTC run v4 by default and do not see a toggle.
- Organizations created before that date can switch the **Preview** toggle (bottom-left in the UI) on and off until Langfuse Cloud becomes v4-only (date will follow).
- Cloud projects created on or after May 20, 2026 are locked to the "enriched observations" export source; new legacy export integrations cannot be created since June 22, 2026.

</Details>

## Questions [#questions]

Ask rollout and migration questions in the dedicated [GitHub Discussion](https://github.com/orgs/langfuse/discussions/12518); we use that thread for updates and answers.

<Cards num={2}>
  <Card
    title="SDK migration guides"
    href="/docs/observability/sdk/upgrade-path"
    icon={<Book />}
  />
  <Card
    title="Versions & Compatibility"
    href="/docs/compatibility"
    icon={<GitCompare />}
  />
  <Card
    title="Working with observations"
    href="/faq/all/explore-observations-in-v4"
    icon={<Table />}
  />
  <Card
    title="Ask questions on Langfuse v4"
    href="https://github.com/orgs/langfuse/discussions/12518"
    icon={<Github />}
  />
</Cards>

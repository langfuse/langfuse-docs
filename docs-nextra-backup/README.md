# Nextra docs backup

This folder is the former `pages/docs/` content (Nextra). It was moved here to resolve the route conflict with the App Router `/docs` route.

**Use it as the source when migrating docs into `content/docs/`** for Fumadocs. See project root `MIGRATION.md` for the migration steps.

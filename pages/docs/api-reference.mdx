import { RedocStandalone } from "redoc";

# API

## API reference

Authenticate with the API using Basic Auth:

- username: Langfuse Public Key
- password: Langfuse Secret Key

<div className="mt-6 mb-20 pt-6 h-[80vh] overflow-x-auto bg-white rounded-lg">
  <RedocStandalone specUrl="https://cloud.langfuse.com/openapi-server.yml" />
</div>

## API reference (public key)

Authenticate with the API using Bearer Auth using only the public key. Meant to be used e.g. directly in the browser.

<div className="mt-6 pt-6 h-[80vh] overflow-x-auto bg-white rounded-lg">
  <RedocStandalone specUrl="https://cloud.langfuse.com/openapi-client.yml" />
</div>

---
title: What is the compatibility between Langfuse UI and Python SDK?
tags: [self-hosting]
---

# What is the compatibility between Langfuse UI and Python SDK?

Generally, the UI is backwards compatible with older SDK versions. However, if you update the SDK, you also need to update the UI to the latest version.